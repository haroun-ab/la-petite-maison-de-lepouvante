# Variables globales pour tous les pipelines
variables:
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2"
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"
  ONLY_BRANCHES:
    - merge_requests
    - main
    - develop
